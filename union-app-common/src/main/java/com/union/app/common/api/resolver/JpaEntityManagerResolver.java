package com.union.app.common.api.resolver;

public class JpaEntityManagerResolver
{

/*

    private static Map<String,ApiTransaction> apiTransactionMap = new ConcurrentHashMap<>();

    @PersistenceContext
    private EntityManager entityManager;

    @Autowired
    private JpaTransactionUtil jpaTransactionUtil;




    public Transaction beginTransaction()
    {
        TransactionStatus transactionStatus = jpaTransactionUtil.newTransaction();
        return new Transaction(transactionStatus);
    }

    public void commit(Transaction transaction, ApiException apiException)
    {

    }





*/












}
